package test.contact

import poc.bpm.Role

class Supervisor extends Role {
  //def breaksToValidate = this.participations.map(p => p.actions).flatten.filter(a => a.activity.)

  //Set[Action[Void, Boolean]] 

}